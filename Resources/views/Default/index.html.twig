{% extends 'base.html.twig' %}


{% block body %}

<h3>File Search plugin</h3>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% if foundFiles %}
    {{ dump(foundFiles) }}
{% else %}
    Nothing found.
{% endif %}
{% endblock %}